services:
  postgres:
    image: postgres:15-alpine
    container_name: ai-scribe-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ai_scribe_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - ai-scribe-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ai-scribe-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL:-postgresql://postgres:password@postgres:5432/ai_scribe_db?schema=public}
      - PORT=${PORT:-3000}
      - OPENAI_API_KEY=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
      - UPLOAD_DIR=${UPLOAD_DIR:-./uploads}
      - MAX_FILE_SIZE=${MAX_FILE_SIZE:-10485760}
    ports:
      - "3000:3000"
    volumes:
      - ./backend/uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ai-scribe-network

volumes:
  postgres_data:

networks:
  ai-scribe-network:
    driver: bridge
